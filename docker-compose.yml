version: "3"

services:
  backend:
    build: .
    ports:
      - "8000:8000"

  frontend:
    build: ./ai-hospital
    ports:
      - "5173:80"

  db:
    image: postgres
    environment:
      POSTGRES_DB: ai_hospital
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
